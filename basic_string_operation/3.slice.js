//The slice() method is used to extract a portion of a string or array without modifying the original.

let originalString = 'Hello World';
let slicedString = originalString.slice(0, 5);

console.log(slicedString); // 'Hello'
